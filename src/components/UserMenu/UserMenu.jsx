const UserMenu = () => {
  return (
    <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: 20,
    }}>
      <p>mango@mail.com</p>
      <button>Logout</button>
    </div>
  );
};

export default UserMenu;
